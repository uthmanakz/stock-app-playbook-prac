global:
 scrape_intervals: 15s

scrape_configs:
 - job_name: prometheus
   static_configs:
    - targets: {% for host in groups['all']%}
                {{ hostvars[host]['public_ip_address'] }}
                 {% endfor %}
