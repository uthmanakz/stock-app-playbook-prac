global:
 scrape_interval: 15s

scrape_configs:
  - job_name: node-exporter
    static_configs:
     - targets: 
      {% for host in groups['dev_nodes'] %}
      - "{{ hostvars[host]['public_ip_address'] }}:9100"
      {% endfor %}
     
